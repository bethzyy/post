# AI图像生成器 - 使用说明

**创建日期:** 2026年2月13日
**版本:** v1.0

## 功能介绍

这是一个基于即梦AI(Seedream)的智能图像生成工具,支持两种输入方式和七种画图风格。

### 核心特性

- ✨ **双模式输入**: 主题描述 OR 参考图片(二选一)
- 🎨 **多种风格**: 国风工笔、国风水墨、水彩画、油画、动漫插画、写实摄影、卡通插画
- 🤖 **智能模型**: 优先使用即梦AI(Seedream),自动降级到Gemini
- 🖼️ **即时预览**: 生成后立即在浏览器中显示
- 💾 **一键下载**: 支持下载生成的图片

## 使用方法

### 方法1: 独立运行(推荐)

```bash
cd C:\D\CAIE_tool\MyAIProduct\post\picture
python standalone_image_generator.py
```

然后在浏览器中打开: **http://localhost:5001**

### 方法2: 通过工具管理器

```bash
cd C:\D\CAIE_tool\MyAIProduct\post
python tool_manager.py
```

在浏览器中打开 http://localhost:5000,找到"节日图像生成"分类中的"🎨 Web图像生成器"

## 操作步骤

### 步骤1: 选择输入模式

**选项A - 主题描述模式**
1. 点击"主题描述"按钮
2. 在文本框中输入您想要生成的图像主题
3. 示例:
   ```
   东北粘火烧外皮金黄酥脆,内里软糯拉丝。黄米面做皮,红豆做馅,香甜不腻,
   也是寓意生活'粘'粘乎乎,甜甜蜜蜜 ✨
   ```

**选项B - 参考图片模式**
1. 点击"参考图片"按钮
2. 点击上传区域或拖拽上传参考图片
3. 支持格式: JPG, PNG等常见图片格式

### 步骤2: 选择画图风格

从下拉菜单中选择一种风格:

| 风格 | 描述 | 适用场景 |
|------|------|---------|
| 国风工笔 | 中国传统工笔画风格,线条精细,色彩淡雅 | 传统题材、花鸟、山水 |
| 国风水墨 | 中国水墨画风格,意境深远,水墨淋漓 | 写意山水、传统题材 |
| 水彩画 | 色彩通透,轻盈自然 | 文艺题材、插画 |
| 油画 | 色彩丰富,笔触明显 | 古典题材、艺术创作 |
| 动漫插画 | 日式动漫风格,色彩鲜明 | 人物、Q版、可爱主题 |
| 写实摄影 | 真实照片风格,细节丰富 | 产品图、实景照片 |
| 卡通插画 | 可爱卡通风格,色彩明快 | 儿童题材、吉祥物 |

### 步骤3: 开始生成

1. 点击"开始生成"按钮
2. 等待约30-60秒(取决于网络和模型响应速度)
3. 生成完成后会自动显示在右侧面板
4. 点击"下载图片"按钮保存到本地

## 生成的文件

生成的图片保存在:
```
picture/generated_images/YYYYMMDD_HHMMSS/generated_风格_时间戳.png
```

例如:
```
picture/generated_images/20260213_143022/generated_guofeng_gongbi_20260213_143022.png
```

## 技术架构

### 后端 (Python)

**文件:** `picture/standalone_image_generator.py`

使用Flask框架,提供:
- `/` - 主页面(Web界面)
- `/api/generate-image` - 生成图像API

### 前端 (HTML/JS)

**文件:** `picture/web_image_generator.html`

单页面应用(SPA),包含:
- 双模式输入切换
- 图片上传预览
- 风格选择下拉框
- 生成状态显示
- 图片展示和下载

### AI模型调用顺序

1. **第一优先级**: 即梦AI(Seedream) - `doubao-seedream-4-5-251128`
   - 配置: `VOLCANO_API_KEY` (在 `.env` 文件中)
   - 特点: 无AI水印,画质好

2. **备选方案**: Gemini - `gemini-3-pro-image-2k`
   - 配置: `ANTIGRAVITY_API_KEY` (在 `.env` 文件中)
   - 特点: 画质最高,但有配额限制

## API参数说明

### 请求格式 (POST /api/generate-image)

```json
{
  "mode": "theme",              // 模式: "theme"(主题) 或 "reference"(参考图片)
  "theme": "东北粘火烧...",      // 主题描述(主题模式必填)
  "reference_image": "base64...", // 参考图片的base64编码(参考图片模式必填)
  "style": "guofeng_gongbi"    // 风格代码
}
```

### 响应格式

**成功:**
```json
{
  "success": true,
  "message": "图像生成成功",
  "model": "seedream",           // 使用的模型: seedream 或 gemini
  "style": "国风工笔",
  "prompt": "...",              // 实际使用的提示词
  "image_path": "完整路径",
  "image_filename": "文件名.png",
  "image_base64": "base64编码的图片数据",
  "timestamp": "2026-02-13 14:30:22"
}
```

**失败:**
```json
{
  "success": false,
  "error": "错误描述信息"
}
```

## 配置检查

使用前请确保 `.env` 文件中配置了以下API密钥:

```bash
# 即梦AI(Seedream) - 必需
VOLCANO_API_KEY=你的火山引擎API密钥

# Gemini - 可选(作为备选方案)
ANTIGRAVITY_API_KEY=你的anti-gravity密钥
ANTIGRAVITY_BASE_URL=http://127.0.0.1:8045/v1
```

## 常见问题

### Q1: 点击"开始生成"后提示"Volcano客户端未配置"
**A:** 请检查 `.env` 文件中的 `VOLCANO_API_KEY` 是否已正确配置

### Q2: 生成速度很慢
**A:** 正常情况下需要30-60秒,取决于:
- 网络连接速度
- 模型服务器负载
- 图片分辨率(2K分辨率需要更多时间)

### Q3: 生成的图片质量不理想
**A:** 可以尝试:
1. 换一个画图风格
2. 优化主题描述,更详细具体
3. 如果使用的是Gemini(备选方案),等待Seedream恢复后重试

### Q4: 如何查看更多生成历史?
**A:** 生成的所有图片都保存在 `picture/generated_images/` 目录下,按时间戳组织

## 更新日志

### v1.0 (2026-02-13)
- ✅ 初始版本发布
- ✅ 支持主题描述和参考图片两种输入模式
- ✅ 支持7种画图风格
- ✅ 集成即梦AI(Seedream)和Gemini双模型
- ✅ Web界面,实时预览和下载

## 技术支持

如有问题,请检查:
1. Python版本 >= 3.8
2. 已安装依赖: `flask`, `openai`, `requests`
3. `.env` 文件配置正确
4. 网络连接正常

---

**享受AI创作的乐趣!** 🎨✨
